# Fichier de règles de filtrage pour filter_posts.py
# Chaque section contient des règles d'un même type.
# Pour ajouter une règle, il suffit d'en ajouter une ligne dans la section correspondante.

# ── Suppressions ──────────────────────────────────────────────────────────────
# Supprime le POST ENTIER si le titre correspond au motif (insensible à la casse).
[delete]
a \u00c3\u00a9crit sur le profil de
a écrit sur le profil de

# ── Remplacements de texte ────────────────────────────────────────────────────
# Remplace toutes les occurrences de la chaîne par xxxxx (insensible à la casse).
# Les mentions @[...] sont automatiquement remplacées par xxxxx.
[replace]
Salim Benfarhat
